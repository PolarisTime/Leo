# 合同模块归档

采购合同与销售合同模块自 Flyway `V71` 起退役。

- 本目录保留退役时的后端源码和打印布局，仅用于历史追溯。
- `archive/contracts` 不属于 Maven 源码或运行时资源目录，不得从生产代码引用。
- 历史合同表和业务数据继续保留；菜单、权限、编号规则与打印模板已禁用。
- 如需恢复模块，应重新评估现有业务模型并通过新的 Flyway 迁移恢复，不得直接搬回旧代码。
